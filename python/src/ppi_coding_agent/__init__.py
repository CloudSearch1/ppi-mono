"""Coding-agent product layer for the Python rewrite."""

from __future__ import annotations

# Keep package import lightweight so lower-level modules can import subpackages
# without pulling in CLI/bootstrap dependencies.
